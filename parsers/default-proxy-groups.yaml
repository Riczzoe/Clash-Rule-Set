      prepend-proxy-groups: # 建立策略组

        - name: PROXY
          type: select
          proxies:

        - name: AI
          type: select
          proxies:

        - name: Apple
          type: select
          proxies:

        - name: Amazon
          type: select
          proxies:

        - name: Bilibili
          type: select
          proxies:

        - name: China
          type: select
          proxies:

        - name: Coursera
          type: select
          proxies:

        - name: Disney
          type: select
          proxies:

        - name: Emby
          type: select
          proxies:

        - name: Google
          type: select
          proxies:

        - name: HamiVideo
          type: select
          proxies:

        - name: HBOMAX
          type: select
          proxies:

        - name: Microsoft
          type: select
          proxies:

        - name: Netflix
          type: select
          proxies:

        - name: PayPal
          type: select
          proxies:

        - name: Scholar
          type: select
          proxies:

        - name: Spotify
          type: select
          proxies:
 
        - name: Steam
          type: select
          proxies:

        - name: StarPlusLogin
          type: select
          proxies:

        - name: StarPlus
          type: select
          proxies:

        - name: Speedtest
          type: select
          proxies:

        - name: Twitter
          type: select
          proxies:

        - name: Telegram
          type: select
          proxies:

        - name: Tiktok
          type: select
          proxies:

        - name: YouTube
          type: select
          proxies:

        - name: Reject
          type: select
          proxies:
            - REJECT
            - PROXY

        - name: Final
          type: select
          proxies:
            - PROXY
            - DIRECT