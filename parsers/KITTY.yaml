      append-proxy-groups: # 建立策略组

        - name: HK 
          type: select

        - name: TW
          type: select

        - name: SG 
          type: select

        - name: JP
          type: select

        - name: US
          type: select
        
        - name: Asia
          type: select

      commands:
        - proxy-groups.HK.proxies=[]proxyNames|^(.*)(HK|香港|澳门)+(.*)$ # 向指定策略组添加订阅中的节点名，可使用正则过滤     
        - proxy-groups.TW.proxies=[]proxyNames|^(.*)(TW|台湾)+(.*)$  
        - proxy-groups.SG.proxies=[]proxyNames|^(.*)(SG|新加坡)+(.*)$ 
        - proxy-groups.JP.proxies=[]proxyNames|^(.*)(JP|日本)+(.*)$ 
        - proxy-groups.US.proxies=[]proxyNames|^(.*)(US|美国)+(.*)$ 
        - proxy-groups.Asia.proxies=[]proxyNames|^(.*)(India|Indonesia|BD|ID|马来西亚|马尔代夫|柬埔寨|泰国|缅甸|老挝|越南|文莱|朝鲜|关岛|斐济|菲律宾|印尼|印度|蒙古|约旦|伊朗|巴林|阿曼|以色列|土耳其|尼泊尔|孟加拉|黎巴嫩|伊拉克|叙利亚|卡塔尔|阿联酋|阿塞拜疆|亚美尼亚|格鲁吉亚|巴基斯坦|斯里兰卡|沙特阿拉伯|哈萨克斯坦|吉尔吉斯斯坦|乌兹别克斯坦)+(.*)$
        # 一些可能用到的正则过滤节点示例，使分组更细致
        # []proxyNames|a                         # 包含a
        # []proxyNames|^(.*)(a|b)+(.*)$          # 包含a或b
        # []proxyNames|^(?=.*a)(?=.*b).*$        # 包含a和b
        # []proxyNames|^((?!b).)*a((?!b).)*$     # 包含a且不包含b
        # []proxyNames|^((?!b|c).)*a((?!b|c).)*$ # 包含a且不包含b或c
